procs:
  world:
    cwd: packages/world
    shell: pnpm run dev
  explorer:
    cwd: packages/world
    shell: pnpm explorer --indexer-port 3008
  anvil:
    shell: anvil --block-time 2 --base-fee 50 --gas-limit 100000000
