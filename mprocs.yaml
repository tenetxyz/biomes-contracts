procs:
  contracts:
    shell: pnpm run deploy:local
  explorer:
    cwd: packages/world
    shell: pnpm explorer --dev --pi 3008
  anvil:
    shell: anvil --block-time 2 --block-base-fee-per-gas 50 --gas-limit 100000000
